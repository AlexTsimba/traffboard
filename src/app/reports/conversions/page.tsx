import { ProtectedLayout } from "~/components/protected-layout";

export default function ConversionsPage() {
    return (
        <ProtectedLayout>
            <div>Conversions Report</div>
        </ProtectedLayout>
    );
}